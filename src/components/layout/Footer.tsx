import { memo } from "react";

const Footer = memo(function Footer() {
  return (
    <footer className="container mx-auto flex h-12 items-center border-r border-l border-dashed p-4">
      Footer
    </footer>
  );
});

export default Footer;
